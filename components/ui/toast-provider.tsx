"use client";

// This is a placeholder component to avoid breaking other imports
// The real toast functionality is in toast-utils.ts

export function ToastContainer() {
  // Our toast-utils.ts handles the container creation
  // This component just ensures that imports don't break
  return null;
}
